query Categories {
  categories: productCategories {
    nodes {
      id
      image {
        altText
        sizes(size: LARGE)
        sourceUrl(size: LARGE)
        srcSet(size: LARGE)
      }
      name
      slug
    }
  }
}
